import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { AuthProvider } from '@/contexts/AuthContext'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Drawtab | Automated Event Flyer Creation',
  description: 'Create and schedule event flyers that deliver themselves. Perfect for birthdays, anniversaries, and celebrations.',
  keywords: 'flyer creation, event automation, birthday flyers, anniversary cards, automated emails',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <AuthProvider>
          {children}
        </AuthProvider>
      </body>
    </html>
  )
}
